// Assuming this is part of your complete app.js file
// Updating activities and without slices
const activitiesTop = activities.slice(0, 9);
const activitiesBottom = activities.slice(9);
const withoutTop = without.slice(0, 9);
const withoutBottom = without.slice(9);

// Your existing logic follows here...